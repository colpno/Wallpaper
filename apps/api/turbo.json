{
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["PORT", "NODE_ENV", "ENVIRONMENT", "CI"],
      "outputs": ["dist/**"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"]
    },
    "test": {
      "outputs": []
    }
  }
}
